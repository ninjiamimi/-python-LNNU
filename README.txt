Python 期末复习资料库使用说明

重要：不要直接双击 index.html 打开。

直接用 file:// 打开时，浏览器会阻止 Pyodide 动态加载模块，Python 运行区可能无法启动。

正确打开方式：
1. 双击“启动本地服务器.bat”。
2. 浏览器访问：http://localhost:8000/index.html
3. 使用期间不要关闭服务器窗口。
4. 关闭页面后，可以在服务器窗口按 Ctrl + C 结束服务，或直接关闭窗口。

本项目使用本地 pyodide 文件夹，不依赖 CDN。
